<h1>Users</h1>
<br>

<% if User.count == 0 %>
  We don't have any users!
<% else %>
  Total user number: <%= User.count %>
<% end %>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user %>
    </li>
  <% end %>
</ul>

<%= link_to 'New user', new_user_path %>